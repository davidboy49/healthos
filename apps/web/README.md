# Web App

Next.js dashboard for the Personal Health OS.

The first screen is the operational dashboard. It uses fallback preview data when the API is unavailable or unauthenticated so the UI can be developed before Garmin OAuth is connected.
